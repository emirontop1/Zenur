-- [[ WindUI Tabanlı Otomatik Derlenmiş Universal GUI Kütüphanesi ]]
-- Son Derleme Zamanı: 2026-06-17 09:39:01
-- Bu dosya GitHub Actions botu tarafından otomatik üretilmiştir. Elle düzenlemeyiniz.


-- [Modül: Tasarım ve Temalar]
-- Global veya lokal bir tabloda temayı tutuyoruz (Birleştiğinde Init.lua buna erişebilecek)
_G.LibraryTheme = {
    MainBg = Color3.fromRGB(20, 20, 20),
    AccentColor = Color3.fromRGB(43, 108, 176),
    TextColor = Color3.fromRGB(255, 255, 255),
    ElementBg = Color3.fromRGB(30, 30, 30),
    Font = Enum.Font.GothamSemibold
}



-- [Modül: Component - Button]
_G.ButtonComponent = {}

function _G.ButtonComponent.New(parentFrame, text, callback)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(0.9, 0, 0, 35)
    Button.Position = UDim2.new(0.05, 0, 0, 10) 
    Button.Text = text
    Button.TextColor3 = _G.LibraryTheme.TextColor
    Button.BackgroundColor3 = _G.LibraryTheme.AccentColor
    Button.Font = _G.LibraryTheme.Font
    Button.TextSize = 14
    Button.Parent = parentFrame
    
    local UICorner = Instance.new("UICorner", Button)
    UICorner.CornerRadius = UDim.new(0, 6)

    Button.MouseButton1Click:Connect(callback)
    return Button
end


-- [Modül: Component - Toggle]


-- [Modül: Component - Slider]


-- [Modül: Ana Pencere Yönetimi]
local Library = {}

function Library:CreateWindow(title)
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "AutoGeneratedUI"
    
    -- Gelişmiş Executor Korunumu (CoreGui Entegrasyonu)
    local targetParent = game:GetService("CoreGui")
    pcall(function()
        if gethui then targetParent = gethui() end
    end)
    ScreenGui.Parent = targetParent

    -- Ana Panel
    local MainFrame = Instance.new("Frame", ScreenGui)
    MainFrame.Size = UDim2.new(0, 350, 0, 450)
    MainFrame.Position = UDim2.new(0.5, -175, 0.5, -225)
    MainFrame.BackgroundColor3 = _G.LibraryTheme.MainBg
    MainFrame.Active = true
    MainFrame.Draggable = true -- Sürükleme desteği

    -- Başlık Alanı
    local Title = Instance.new("TextLabel", MainFrame)
    Title.Size = UDim2.new(1, 0, 0, 40)
    Title.Text = title
    Title.TextColor3 = _G.LibraryTheme.TextColor
    Title.BackgroundColor3 = _G.LibraryTheme.ElementBg
    Title.Font = _G.LibraryTheme.Font
    Title.TextSize = 16

    -- İçerik Taşıyıcı Frame
    local ContentFrame = Instance.new("Frame", MainFrame)
    ContentFrame.Size = UDim2.new(1, 0, 1, -40)
    ContentFrame.Position = UDim2.new(0, 0, 0, 40)
    ContentFrame.BackgroundTransparency = 1

    -- UI Elementlerini Pencereye Bağlama Fonksiyonları
    function MainFrame:CreateButton(text, callback)
        if _G.ButtonComponent then
            return _G.ButtonComponent.New(ContentFrame, text, callback)
        else
            -- Eğer Actions henüz çalışmadıysa veya hata oluştuysa yedek fallback:
            local fallbackBtn = Instance.new("TextButton", ContentFrame)
            fallbackBtn.Size = UDim2.new(0.9, 0, 0, 35)
            fallbackBtn.Text = text
            fallbackBtn.MouseButton1Click:Connect(callback)
            return fallbackBtn
        end
    end

    return MainFrame
end

return Library

